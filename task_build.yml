---
    platform: linux
    
    image_resource:
      type: docker-image
      source: {repository: node}
    run:
      path: /bin/sh
      args:
        - -c
        - |
          echo "Node Version: $(node --version)"
          echo "NPM Version: $(npm --version)"
          cd concourse-ci
          npm install
          npm test